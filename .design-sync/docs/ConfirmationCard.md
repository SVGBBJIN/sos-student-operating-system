---
category: Confirmations
---

# ConfirmationCard

The core AI confirmation card. Renders a proposed action (add_task, add_event, add_block, delete_*, convert_*, …) with editable fields and Approve / Edit / Dismiss. Colour + icon are keyed off `action.type`.
