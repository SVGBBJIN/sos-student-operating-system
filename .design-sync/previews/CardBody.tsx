import React from 'react';
import { Card, CardHeader, CardBody } from '@sos/design-system';

export function InCard() {
  return (
    <Card>
      <CardHeader title="Outline generated" />
      <CardBody>12 sections covering photosynthesis, cellular respiration, and the Calvin cycle.</CardBody>
    </Card>
  );
}
